spring:
  application:
    name: cnb-springboot

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
  endpoint:
    health:
      probes:
        enabled: true

server:
  port: ${PORT:8080}

app:
  message: Hello Spring Boot! July 1st :11:37
  title: Spring Boot with Cloud-native Buildpacks
